
export NDK=~/devel/android-ndk-r9
export ANDROID_NDK=$NDK
export ANDROID_NDK_HOME=$NDK
export PATH=$NDK:$PATH

# Build SDL
cd sdl
./rebuild.sh

# SDL will create a dummy libapplication.so
# To build the real application.so

cd jni/application/src
./AndroidBuild.sh  armeabi-v7a
./AndroidBuild.sh  x86


